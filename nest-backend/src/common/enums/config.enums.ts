export enum CONFIG {
  AUTH = 'authConfig',
  POSTGRES = 'postgresConfig',
  REDIS = 'redisConfig',
  SERVER = 'serverConfig',
  AWS = 'awsConfig',
  KAFKA = 'kafkaConfig',
  INTERNAL = 'internalConfig',
  OPENSEARCH = 'opensearchConfig',
  FFMPEG = 'ffmpegConfig',
}
